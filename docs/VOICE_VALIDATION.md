# 语音功能联调记录

日期：2026-09-10；设备：ESP32-C3 FoloToy AI Passport；电脑：Windows。

- ESP-IDF 5.5.3 编译通过，应用分区可容纳固件。仍有原先 recovery 分区容量提示，本次只刷写应用分区，保留配网和 NVS。
- 15 项 C 主机测试通过，30 项 Python 测试通过，网页 JavaScript 语法检查通过。
- 新增验证覆盖 IMA ADPCM 参考向量、音频帧边界、并发加密请求、快捷键限制、结束与断连清理、设备语音页操作与亮屏策略。
- 已在中文网页验证讯飞 F6 配置、虚拟设备刷新及未安装驱动时的提示。
- 初版每次只能取 40 ms 音频，在此网络 100–200 ms 往返延迟下出现丢帧。最终版采用独立帧 IMA ADPCM、每次最多取 160 ms 音频、320 ms 有界设备队列。
- 最终固件已刷入并通过写入校验。通过正式控制台“仅测试麦克风”模式观察到两次录音开始／结束，音量峰值最高 6581 / 32768，两次丢帧计数均为 0；结束后连接保持正常。这是短录音测试，不能代表所有网络环境。
- VB-CABLE 已安装，WASAPI 音频流已成功启动；修复了网页枚举音频设备与打开音频流线程不同导致的启动失败，并增加回归测试。用户已确认：设备确认键录音，经 VB-CABLE 和 F6 联动，讯飞成功输出识别文字，Windows + 讯飞端到端联调通过。豆包、Typeless 和 macOS 实机测试未运行。
- 最终版 USB 状态查询两次超时，局域网状态和录音仍正常；未据此声称完成串口或长时间稳定性验收。

录音测试只检查内存中的音量和传输状态，没有保存录音文件。驱动安装包来自 VB-Audio 官网，Windows Authenticode 显示签名有效；用户已安装驱动。
